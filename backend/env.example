# KBase Backend Configuration

# Required: Path to your note vault directory
VAULT_PATH=/path/to/your/vault

# Authentication Configuration
# Required: Secret key for JWT token signing (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-key-here

# Required: Plain text password for authentication
PASSWORD=your-password-here

# Optional: Access token expiration time in minutes (default: 30)
# ACCESS_TOKEN_EXPIRE_MINUTES=30

# Optional: Update CORS settings for production
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

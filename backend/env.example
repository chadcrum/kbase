# KBase Backend Configuration

# Required: Path to your note vault directory
# Example: ~/kbase-vault
VAULT_PATH=~/kbase-vault

# Authentication Configuration
# Optional: Disable authentication (for development/local use only)
# Defaults to true in development mode (ENV=development, ENVIRONMENT=development, or DEBUG=true)
# Set explicitly to override: DISABLE_AUTH=true or DISABLE_AUTH=false
# When set to true, SECRET_KEY and PASSWORD are not required
# DISABLE_AUTH=true

# Required (if auth enabled): Secret key for JWT token signing
# Generate a secure key with: openssl rand -hex 32
SECRET_KEY=your-secret-key-here

# Required (if auth enabled): Plain text password for authentication
PASSWORD=your-password-here

# Optional: Access token expiration time in minutes (default: 10080 = 7 days)
# Set to 30 for 30 minutes, 1440 for 1 day, 10080 for 7 days, etc.
# Note: "Remember Me" option extends this to 30 days
# ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Optional: Server host (default: 0.0.0.0)
# HOST=0.0.0.0

# Optional: Server port (default: 8000)
# PORT=8000

# Optional: Application name (default: KBase)
# APP_NAME=KBase

# Optional: Application version (default: 0.1.0)
# APP_VERSION=0.1.0

# Optional: JWT signing algorithm (default: HS256)
# ALGORITHM=HS256
